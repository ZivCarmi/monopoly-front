import GamePanel from "../GamePanel";
import GameSettings from "./GameSettings";

const SettingsPanel = () => {
  return (
    <GamePanel>
      <GameSettings />
    </GamePanel>
  );
};

export default SettingsPanel;
